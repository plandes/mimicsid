# description: external configuration use (all imports)

[import]
sections = list: msidd_all_imp

[msidd_all_imp]
type = import
config_files = list:
    resource(zensols.mimicsid): resources/pkg/default.conf,
    resource(zensols.mimicsid): resources/pkg/obj.conf
